gentoo specific hacks because we unbundle the
deps of irrlicht

diff --git a/CMakeLists.txt b/CMakeLists.txt
index fbf2770..2bfd75b 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -283,6 +283,10 @@ target_link_libraries(supertuxkart
     ${OGGVORBIS_LIBRARIES}
     ${IRRLICHT_XF86VM_LIBRARY}
     ${OPENAL_LIBRARY}
     ${OPENGL_LIBRARIES}
+    png
+    jpeg
+    bz2
+    z
     )
 
 if(UNIX AND NOT APPLE)
